# set =collection which is unordered, unindexed. No dupicate values

utensils = {"fork", "spoon", "knife"}
dishes = {"bowl", "plate", "cup", "knife"}

#utensils.add("napkin")
#utensils.remove("fork")
#utensils.clear()
#utensils.update(dishes) # add all elements in dishes to utensils set
#dinner_table = utensils.union(dishes) # creats a new set and adds the elements in utensils and dishes
#print(utensils.difference(dishes)) #prints what utensils has that dishes doesn't
print(utensils.intersection(dishes)) #return the element they have in common
#for x in utensils:
 #   print(x)

#for x in dinner_table:
    #print(x)